export type ItemWeight = {
  readonly θ: number;
  readonly w: number;
  readonly i: number;
  readonly d: number;
};

export type User = {
  readonly name: string;
};

export type Item = {
  readonly weight: number;
  readonly value: number;
};
